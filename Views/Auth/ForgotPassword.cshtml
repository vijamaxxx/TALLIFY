@{
    ViewData["Title"] = "Forgot Password";
    Layout = "_AuthLayout";
}

<div class="auth-page">
  <div class="auth-card">
    <div class="auth-logo-wrapper">
      <img src="~/images/LOGO TEXT.png" alt="Logo" class="auth-logo" />
    </div>

    <section class="auth-screen is-active" aria-label="Forgot password">
      <h1 class="auth-title">Forgot password?</h1>
      <p class="auth-subtitle">
        Enter the email associated with your account and we’ll send a reset link.
      </p>

      @if (ViewBag.Error != null)
      {
          <div class="auth-alert auth-alert-error">
              @ViewBag.Error
          </div>
      }

      <form method="post" asp-action="ForgotPassword" asp-controller="Auth" class="auth-form">
        @Html.AntiForgeryToken()
        
        <div class="auth-floating-group">
            <input id="fp-email"
                   name="email"
                   type="email"
                   class="auth-input"
                   placeholder=" "
                   required />
            <label class="auth-floating-label" for="fp-email">Email</label>
        </div>

        <div class="auth-actions-single">
          <button type="submit" class="auth-button auth-button-primary">
            Send reset link
          </button>
        </div>
      </form>

      <p class="auth-bottom-text">
        Remembered your password?
        <a asp-controller="Auth" asp-action="Login" class="auth-link-inline">Back to login</a>
      </p>
    </section>
  </div>
</div>
